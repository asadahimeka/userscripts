{
  "name": "ymail-clean",
  "version": "0.1.1",
  "license": "MIT",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "build:add": "pn build && git add .",
    "release": "bumpp -x \"pn build:add\" -c \"release ymail-clean@%s\" -t \"ymail-clean@%s\" -p"
  },
  "dependencies": {
    "@types/arrive": "2.4.1"
  }
}
